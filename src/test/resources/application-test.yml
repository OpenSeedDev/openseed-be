spring:
  main:
    banner-mode: off

  jpa:
    show-sql: false

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false

app:
  ranking:
    scheduling-enabled: false
  auth:
    jwt-secret: test-signing-key-with-at-least-32-bytes
    cookie-secure: false
